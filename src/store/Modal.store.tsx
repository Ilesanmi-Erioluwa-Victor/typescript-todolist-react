import { createSlice } from "@reduxjs/toolkit";

const initialState: {
  modalCreateTaskOpen: false;
};

const modalSlice = createSlice({
    name: "modal",
    initialState,
    reducers: {
        
    }
})
